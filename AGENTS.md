# AGENTS.md

## 프로젝트별 AI 작업 규칙

- 이 프로젝트는 Student AI OS 가설 검증을 위한 신규 프로젝트다.
- Codex는 작업 전에 `AGENTS.md`, `SPEC.md`, `ARCH.md`, `TASK.md`, `README.md`를 먼저 읽는다.
- 회사 공통 규칙(`~/.codex/AGENTS.md`, `/home/merlin/hq/company-os.md`)과 이 프로젝트 규칙을 함께 따른다.
- `TASK.md` 범위 밖 작업은 하지 않는다.
- 큰 작업은 본 프로젝트가 아니라 `worktrees/studentAI/<task-id>/`에서 먼저 진행한다.
- 테스트 통과 전에는 완료 보고하지 않는다.

## 작업 전 확인

- 현재 작업이 새 프로젝트 셋업인지 기존 기능 확장인지 먼저 판단한다.
- 구현 전에 기존 문서와 테스트 구조를 확인한다.
- 기능 변경에 테스트가 없으면 최소 회귀 테스트를 먼저 추가한다.
- 민감정보(API 키, 토큰, 개인정보, `.env` 값)는 코드나 문서에 저장하지 않는다.

## 테스트 / 검증 규칙

1. 문서 작업만 있을 때도 변경 파일과 남은 리스크를 보고한다.
2. 앱 스캐폴드가 생기면 `package.json`의 실제 스크립트를 기준으로 검증한다.
3. `npm run check`가 존재하면 반드시 통과시킨다.
4. `npm run check`가 없으면 실제 존재하는 `lint`, `typecheck`, `test`, `build`만 실행한다.
5. 존재하지 않는 명령을 억지로 만들거나 실행하지 않는다.
6. 검증 실패 시 원인을 수정하고 재실행한다.

## 완료 보고

마지막 보고에는 아래를 포함한다.

- 변경 파일
- 실행 명령
- 테스트 결과
- 남은 리스크
- 사용자 승인 필요 항목
